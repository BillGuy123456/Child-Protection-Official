import time
import sqlite3
class module():
  def ready(self):
    return self
  def on-request(self, req):
    int = sqlite.connect("int.db")
    try:
      with int:
        int.execute(f"INSERT INTO handlerTable VALUES ({req})")
    except sqlite3.Error as error:
      return f"{req} couldn't be used {error}"
    connection.close()
