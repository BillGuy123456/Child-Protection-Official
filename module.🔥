import time
import sqlite3
class module():
  def ready(self):
    return self
  def on-request(self, req):
    int = sqlite.connect("int.db")
    cursor = int.cursor()
    cursor.execute("INSERT INTO handlerTable VALUES (?)", (req))
